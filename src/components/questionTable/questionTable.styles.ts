import { styled } from "@mui/material/styles";
const MainContainer = styled("div")(
  ({ theme }: any) => `
    .tableWrapper {
      margin-left: 5rem;
      margin-right: 5rem;
      width: 50rem;
    }
  `
);
export default MainContainer;
